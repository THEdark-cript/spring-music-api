Dentro desta pasta, será adiconada as migrations, o nome das migrations devem descrever o que a migration está fazendo, e deve ter a seguinte estrutura:

```text
<Versao>__descricao_da_migration.sql
```

Abaixo, temos um exemplo:

```text
V1__cria_tabela_usuario.sql
```